# Local testing configuration for Nonagon API & bot.
# Copy or adjust values if your test environment differs.

# Mongo used by pytest fixtures and local API runs
TEST_MONGODB_URI=mongodb://root:example@localhost:27017/?authSource=admin
TEST_DB_NAME=nonagon_test
TEST_ADMIN_TOKEN=test-admin-token

# Default application connection (matches docker-compose.dev.yml)
MONGODB_URI=mongodb://root:example@localhost:27017/?authSource=admin
DB_NAME=nonagon
API_ADMIN_TOKEN=test-admin-token
